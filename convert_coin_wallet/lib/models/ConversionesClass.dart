class Conversion {
  final String fromCurrency;
  final double fromValue;
  final String toCurrency;
  final double toValue;

  Conversion(this.fromCurrency, this.fromValue, this.toCurrency, this.toValue);
}
